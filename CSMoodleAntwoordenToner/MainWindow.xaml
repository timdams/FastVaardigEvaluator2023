<Window x:Class="CSMoodleAntwoordenToner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSMoodleAntwoordenToner"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Name="allWindow" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <!-- Header -->
            <RowDefinition></RowDefinition>
            <!-- Main -->
            <RowDefinition Height="auto"></RowDefinition>
            <!-- footer -->
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Name="gridHeader">
            <DockPanel>
                <Menu DockPanel.Dock="Top">
                    <MenuItem Header="_Load" Click="Load_MenuItem_Click"  >
                    </MenuItem>


                </Menu>
            </DockPanel>
        </Grid>

        <Grid Grid.Row="1" Name="gridMain">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="338*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="135*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="575*"></ColumnDefinition>
                <ColumnDefinition Width="236*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Name="gridLeftSolutions" Grid.ColumnSpan="2" Margin="0,0,8,0" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition></RowDefinition>

                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">

                </StackPanel>
                <ListBox Grid.Row="1"    Name="lbSollist" SelectionChanged="lbSollist_SelectionChanged" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Achternaam}"></TextBlock>
                                <TextBlock Text=" "></TextBlock>
                                <TextBlock Text="{Binding Voornaam}"></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </Grid>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Center" Background="DarkGray" ShowsPreview="True" Width="8"       />
            <Grid Grid.Column="2" Name="gridMiddleSolutions" DataContext="{Binding SelectedSolution.SelectedProject}"  >
                <ListBox Name="lbAntwoorden" SelectionChanged="lbSollist_SelectionChanged" >

                </ListBox>
            </Grid>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Center" Background="DarkGray" ShowsPreview="True" Width="8"       />
            <Grid Grid.Column="3" Name="gridRightSolutions" DataContext="{Binding SelectedSolution.SelectedProject.SelectedFile}" Grid.ColumnSpan="3" Margin="8,0,0,0">
                <!--<Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Name="puntenGrid" DataContext="{Binding ElementName=lbSollist, Path=DataContext.SelectedSolution}" Grid.Row="0">
                <ListBox ItemsSource="{Binding EvaluatieRapport.Evaluaties}"  ></ListBox>
            </Grid>-->
                <avalonEdit:TextEditor Grid.Row="1"  xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit" 
                                    Name="textEditor"
                                    FontFamily="Consolas"
                                    SyntaxHighlighting="C#"
                                    FontSize="10pt" WordWrap="True" ShowLineNumbers="True" IsReadOnly="True"
                                    
                                    
                                    />

            </Grid>
        </Grid>
        <Grid Grid.Row="2" Name="gridFooter" >

        </Grid>
    </Grid>
</Window>
